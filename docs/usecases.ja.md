[English](usecases.md) | **日本語**

# ユースケース

## 1. 純粋 ABM ベースライン (LLM なし)

論文の決定論的較正パス．LLM は一切呼ばれない．

```bash
cargo run --release -- run --dataset metoo --abm bc --core-ratio 0.0 \
    --n-agents 1000 --steps 14 --network ba --seed 42
```

4 つの意見力学モデルを比較する — BC/HK は合意へ収束 (低 `polarization`)，SJ/Lorenz は二極化 (高 `polarization`・大きな `mobilized`):

```bash
for m in bc hk sj lorenz; do
  cargo run --release -- run --abm $m --core-ratio 0.0 --n-agents 1000 --steps 14 --seed 42
done
```

## 2. ハイブリッド (LLM コア + ABM 周辺)

ローカル Ollama とモデルの pull が必要．コア層の LLM 駆動態度が一般層へ伝播し (一方向結合)，純粋 ABM のトレンドを補正する．

```bash
ollama pull llama3.2:latest
OLLAMA_HOST=http://localhost:11434 OLLAMA_MODEL=llama3.2:latest \
cargo run --release -- run --dataset metoo --abm bc --core-ratio 0.3 \
    --n-agents 1000 --steps 14 --network ba --llm-temperature 0 --seed 42
```

初回実行で `.llm_cache/cache.json` が埋まり，同一コマンドの再実行はキャッシュから再生され (cache-hit 100%・LLM 非呼び出し)，結果を完全に再現する．

## 3. オフライン ハイブリッドスモーク (ライブ LLM 不要)

CI・ネットワーク遮断環境向け: `ScriptedClient` mock がコア層をハイブリッドパイプライン全体で駆動する．

```bash
cargo run --release --example mock_smoke -- results
```

## 4. 感度分析スイープ

```bash
cargo run --release -- sweep \
    --core-ratio-min 0.0 --core-ratio-max 0.5 --core-ratio-step 0.1 \
    --abm-values bc,hk,sj,lorenz --network-values ba,ws,er --runs 10 --seed 42
uv run hisim-tools visualize-sweep
```

期待される定性的知見: 小さなコア比率でもマクロトレンドが補正される; BA は WS/ER よりコア影響を増幅する; SJ/Lorenz は二極化し BC/HK は合意に至る．

---
*This file was generated by Claude Code.*
